import 'package:flutter/material.dart';

class PopUpSetting extends StatelessWidget {
  const PopUpSetting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Drawer(
      backgroundColor: Colors.red,
    );
  }
}
